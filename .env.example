# Backend de dados do Finer One (Google Apps Script, Vercel API, etc.).
# Deixar vazio para usar o mockData da Overcel (a app funciona na mesma).
# Quando definido, blingDataService consome GET {BASE}/pedidos/vendas.
VITE_API_BASE_URL=
